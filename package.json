{
  "name": "prinstine-management-system",
  "version": "1.0.0",
  "description": "Comprehensive management system for Prinstine Group of Companies",
  "scripts": {
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "server": "cd server && npm run dev",
    "client": "cd client && npm start",
    "build": "cd client && npm install && npm run build",
    "build:client": "cd client && npm run build",
    "install-all": "cd server && npm install && cd ../client && npm install"
  },
  "keywords": [
    "prinstine",
    "management",
    "system"
  ],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "dependencies": {
    "date-fns": "^4.1.0"
  }
}
